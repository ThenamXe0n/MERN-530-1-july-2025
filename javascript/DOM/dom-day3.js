// key or property of dom element

// id, className, name, tagname, type, value, href, src, style, title, alt, etc attribute

// innerHTML, innerText, textContent,classList


let selectedElement =document.getElementById("main_container")
let imageElement =document.getElementById("myImage")
let inputElement =document.getElementById("myInput")
let linkElement =document.getElementById("myLink")